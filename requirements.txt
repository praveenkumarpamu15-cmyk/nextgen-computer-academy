# Root requirements.txt — required by Vercel Python builder.
# Mirrors backend/requirements.txt so `@vercel/python` can install
# dependencies from the project root when it processes backend/server.py.
#
# IMPORTANT: emergentintegrations lives on Emergent's private PyPI index.
# You MUST set the following environment variable in your Vercel project
# (Settings → Environment Variables) for the build to succeed:
#
#   PIP_EXTRA_INDEX_URL = https://d33sy5i8bnduwe.cloudfront.net/simple/
#

fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
pymongo==4.6.3
motor==3.3.1
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
bcrypt==4.1.3
requests>=2.31.0
python-multipart>=0.0.9
resend>=2.0.0
openpyxl>=3.1.5
emergentintegrations==0.2.0
